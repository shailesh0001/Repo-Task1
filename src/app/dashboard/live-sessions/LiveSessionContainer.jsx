import React, { Fragment } from "react";
import Header from "../common/components/Header";
import Card from "../common/components/Card";

const LiveSessionContainer = () => {
    return (
      <Fragment>
        <Header />
        <Card />
      </Fragment>
    );
};

export default LiveSessionContainer;
